services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=accenture_challenge'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432'
